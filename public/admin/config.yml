// Decap CMS config for Mossroom
// https://decapcms.org/docs/configuration-options/

backend:
  name: github
  repo: YOUR-GITHUB-USERNAME/mossroom-co   # <-- change this
  branch: main
  # If you use a self-hosted OAuth proxy (Cloudflare Worker or hosted service),
  # set base_url below. Otherwise remove this line to use the default Decap OAuth.
  # base_url: https://YOUR-OAUTH-WORKER.workers.dev

# Where uploaded media lives. We use Cloudflare R2 via a custom proxy,
# but for simplicity the local_image_backend keeps uploads inside the repo
# (free, fine for MVP). Switch to R2 once you exceed 1GB.
media_folder: "public/uploads"
public_folder: "/uploads"

publish_mode: editorial_workflow

# Editorial workflow: drafts → in review → ready
# Authors create drafts, editor approves, then publishes.

collections:
  - name: "articles"
    label: "Articles"
    label_singular: "Article"
    folder: "src/content/articles"
    create: true
    delete: true
    slug: "{{slug}}"
    extension: "md"
    format: "frontmatter"
    summary: "{{title}} — {{category}} — {{pubDate | date('YYYY-MM-DD')}}"
    sortable_fields: ["title", "category", "pubDate", "draft"]
    view_filters:
      - label: "Drafts"
        field: draft
        pattern: true
      - label: "Published"
        field: draft
        pattern: false
      - label: "Care guides"
        field: category
        pattern: "care-guide"
      - label: "Plant profiles"
        field: category
        pattern: "plant-profile"
      - label: "Sourcing"
        field: category
        pattern: "sourcing"
      - label: "Problem solving"
        field: category
        pattern: "problem-solving"
    fields:
      - { label: "Title", name: "title", widget: "string" }
      - { label: "Short description", name: "description", widget: "text", pattern: ['^.{50,180}$', 'Description must be 50–180 chars'] }
      - { label: "Category", name: "category", widget: "select", options: ["care-guide", "plant-profile", "sourcing", "problem-solving", "inspiration", "beginner"] }
      - { label: "Difficulty", name: "difficulty", widget: "select", options: ["beginner", "intermediate", "advanced"], required: false }
      - { label: "Tags", name: "tags", widget: "list", required: false }
      - { label: "Publish date", name: "pubDate", widget: "datetime" }
      - { label: "Updated date", name: "updatedDate", widget: "datetime", required: false }
      - { label: "Author", name: "author", widget: "string", default: "Mossroom Team" }
      - { label: "Reading time (minutes)", name: "readingTime", widget: "number", required: false, value_type: "int", min: 1, max: 60 }
      - { label: "Cover image", name: "cover", widget: "image", required: false }
      - { label: "Cover alt text", name: "coverAlt", widget: "string", required: false }
      - { label: "Featured on homepage?", name: "featured", widget: "boolean", default: false }
      - { label: "Draft (not published)", name: "draft", widget: "boolean", default: true }
      - { label: "Body", name: "body", widget: "markdown" }